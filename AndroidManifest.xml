<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidhive.googleplacesandmaps"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Access Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- <meta-data -->
        <!-- android:name="com.google.android.gms.version" -->
        <!-- android:value="@integer/google_play_services_version" /> -->

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/app_id" />

        <uses-library android:name="com.google.android.maps" />

        <!-- Add Google Map Library -->
        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".InitialActivity"
            android:label="@string/title_activity_main" >
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchPageActivity"
            android:label="Place Details" >
        </activity>
        <!-- SinglePlaceActivity -->
        <activity
            android:name=".SinglePlaceActivity"
            android:label="Place Details" >
        </activity>

        <!-- PlacesMapActivity -->
        <activity
            android:name=".PlacesMapActivity"
            android:label="Near Places Map View" >
        </activity>
        <activity
            android:name="com.facebook.LoginActivity"
            android:label="@string/app_name" />
        <activity android:name="cognito.UserPref" >
        </activity>
        <activity
            android:name=".ChooseActivity"
            android:label="@string/title_activity_choose" >
        </activity>
        <activity
            android:name="androidlistview.AndroidListViewActivity"
            android:label="Android List View" >
        </activity>
        <activity
            android:name="androidlistview.SingleListItem"
            android:label="Single Item Selected" >
        </activity>
        <activity android:name="androidlistview.SingleplaceItem" >
        </activity>
        <activity android:name="login.SignUp" >
        </activity>
        <activity android:name="androidlistview.MainActivity5" >
        </activity>
        <activity
            android:name=".SettingActivity"
            android:label="@string/title_activity_setting" >
        </activity>
        <activity
            android:name="androidlistview.AddPlaceActivity"
            android:label="@string/add_place_activity" >
        </activity>
    </application>

</manifest>